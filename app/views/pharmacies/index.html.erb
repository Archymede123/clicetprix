<div class="result-container">
  <div class="results">
    <div class="results__search-bar" style="background-image: linear-gradient(-45deg, #FED63E 50%, #FFED72 100%);">
      <div class="pt-- pl--">
        <%= link_to root_path do %>
          <%= image_tag "logo.png" %>
        <% end %>
      </div>
      <p class="pt pl25">Envoyez une photo de votre ordonnance,<br>retrait express de votre commande en 2 heures !</p>
      <div class="pl25 pt pr">
        <%= render 'shared/research-button' %>
      </div>
    </div>
    <div class="results__list" style="background-image: linear-gradient(-180deg, #FFFEFB 0%, #FFFCF0 100%);">
      <p class="pl25 pt">Les pharmacies Clic & Prix proches de chez vous :</p>
      <div class="card-container pt pl- pr-">
        <%= render 'shared/card' %>
        <%= render 'shared/card' %>
        <%= render 'shared/card' %>
        <%= render 'shared/card' %>
        <%= render 'shared/card' %>
        <%= render 'shared/card' %>
      </div>
    </div>
  </div>

  <div id="map" style="width: 100%; height: 100vh;"></div>

</div>


<!-- <div class="map">
  <div class="map__gmap">
    <div id="map" style="width: 100%; height: 100%;"></div>
  </div>
</div> -->

<% content_for(:after_js) do %>
  <script type="text/javascript">
    var handler = Gmaps.build('Google');
    handler.buildMap({ internal: { id: 'map' } }, function() {
      markers = handler.addMarkers(<%= raw @hash.to_json %>);
      handler.bounds.extendWith(markers);
      handler.fitMapToBounds();
      if (markers.length == 0) {
        handler.getMap().setZoom(2);
      } else if (markers.length == 1) {
        handler.getMap().setZoom(14);
      }
    });
  </script>
<% end %>
